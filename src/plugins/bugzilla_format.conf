# Empty lines and lines starting with # are ignored.
#
# Format:
# Text: element1[,element2]...
#
# Text is the literal text line to be added to Bugzilla comment.
#
# Elements can be:
# - problem directory element names, which get formatted as
#   <element_name>: <contents>
#   or
#   <element_name>:
#   :<contents>
#   :<contents>
#   :<contents>
# - problem directory element names prefixed by "%bare_",
#   which get formatted as-is, without "<element_name>:" and colons
# - %oneline, %multiline, %text wildcards, which select all corresponding
#   elements for output
# - problem directory element names prefixed by "-",
#   which excludes given element from all wildcards

Description of problem: %bare_comment
Version-Release number of selected component: %bare_package
Additional info: -pkg_arch,-pkg_epoch,-pkg_name,-pkg_release,-pkg_version,%oneline,%short_backtrace
