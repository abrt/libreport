GitlabProject = @GITLAB_DEFAULT_PROJECT@
GitlabURL = @GITLAB_DEFAULT_URL@
# Go to Settings > Access Tokens. Screen "Add a personal access token" pops up.
# Select "api" in Scopes. Set the name and expiration date if desired > Create
# personal token. Copy and paste the generated token below.
PrivateToken =
# List of components whose problems we will allow to be reported to the GitLab instance.
Allowlist = @GITLAB_DEFAULT_ALLOWLIST@
