# This is a global configuration used by all libreport plugins

# A colon separated list of element names used by libreport plugins to exclude
# the listed problems element from reports. If you do not want to include some
# file in reports add it on this list.
#
# AlwaysExcludedElements =
